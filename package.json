{
  "name": "rock-paper-scissors",
  "private": true,
  "description": "RPS Online monorepo — use npm start for the AI bot",
  "scripts": {
    "start": "npm run dev --prefix ai",
    "start:ai": "npm run dev --prefix ai",
    "build:ai": "npm run build --prefix ai",
    "pull-models": "npm run pull-models --prefix ai",
    "test:ai": "npm test --prefix ai"
  }
}
