## What's new

- **Stuck in match queue** — Fixes a case where the queue timer kept running but matchmaking never progressed: the app no longer marks you as queued locally after the server queue doc is gone, recovery re-joins or syncs correctly, and an assigned lobby/active match is detected from the server instead of spinning forever.

**Full Changelog**: https://github.com/melkor217/rock-paper-scissors/compare/v0.7.54...v0.7.55
