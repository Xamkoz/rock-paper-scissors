## What's new

- **Match found notification** — Stays in the notification shade until you open the game screen (not dismissed when the app merely comes to foreground). Background coordinator posts the alert when a lobby match is assigned.
- **Launch / loading** — Fixes the home screen stuck on a spinner after opening from a match notification; navigation follows launch intents reliably with an 8s fallback.
- **Live ELO row** — Pre-match ratings with win/loss swings shown as signed deltas (e.g. +20 / −20 on a shutout lead). Server finalization uses match snapshot ratings and round-win counts for shutout bonus (functions deployed).

**Full Changelog**: https://github.com/melkor217/rock-paper-scissors/compare/v0.7.51...v0.7.52
